---
title: ButtonGroup
---

import {ButtonGroup} from '@carrot-components/button';
import {Example, PropTable} from '../../components';

Buttons let users take actions and make choices with a single click or tap.

### Example

<Example>
  <ButtonGroup>
    <ButtonGroup.Button variant="secondary">Delete Draft</ButtonGroup.Button>
    <ButtonGroup.Button variant="primary">Save Draft</ButtonGroup.Button>
  </ButtonGroup>
</Example>

<PropTable component={ButtonGroup} />
<PropTable component={ButtonGroup.Button} />
