import {Button} from '@design-system-example/button';
import {PropTable} from '../../components/PropTable';

# Button

A button!

Here's an example:

<Button>Buy now!</Button>

<PropTable component={Button}/>
