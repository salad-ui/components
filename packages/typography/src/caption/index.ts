import {fontFamily, fontWeightNormal} from '../common';

export const caption = () => `
  ${fontFamily}
  ${fontWeightNormal}
  color: currentColor;
  font-size: 12px;
  line-height: 16px;
  letter-spacing: 0;
`;
