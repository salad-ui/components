---
title: Button
---

import {Button} from '@design-system-example/components';
import {Example, PropTable} from '../../components';

Buttons let users take actions and make choices with a single click or tap.

### Example

<Example>
  <Button variant="tertiary">Save Draft</Button>{' '}
  <Button variant="secondary">Preview</Button>{' '}
  <Button variant="primary">Publish</Button>
</Example>

### Sizes

<Example>
  <Button variant="tertiary" size="lg">
    Save Draft
  </Button>{' '}
  <Button variant="secondary" size="lg">
    Preview
  </Button>{' '}
  <Button variant="primary" size="lg">
    Publish
  </Button>
  <br />
  <br />
  <Button variant="tertiary" size="md">
    Save Draft
  </Button> <Button variant="secondary" size="md">
    Preview
  </Button> <Button variant="primary" size="md">
    Publish
  </Button>
  <br />
  <br />
  <Button variant="tertiary" size="sm">
    Save Draft
  </Button>{' '}
  <Button variant="secondary" size="sm">
    Preview
  </Button>{' '}
  <Button variant="primary" size="sm">
    Publish
  </Button>
</Example>

<PropTable component={Button} />
