import * as React from 'react';
import {PageTemplate} from '~components/PageTemplate';

const SettingsPage = () => {
  return <PageTemplate content={<>Settings</>} />;
};

export default SettingsPage;
