{
  "extends": "./tsconfig.base.json",
  "compilerOptions": {
    /* load source files rather than compile files */
    "baseUrl": ".",
    "paths": {
      "@salad-ui/*": ["packages/*/src"]
    }
  },
  "include": ["**/*.ts", "**/*.tsx", "**/*.d.ts"]
}
